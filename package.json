{
  "name": "docker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "node ./bin/www dev",
    "build": "docker buildx build  --platform linux/amd64,linux/arm64 -t joshuapeddle/notes-app:0.1 .",
    "push": "docker buildx build  --platform linux/amd64,linux/arm64 -t joshuapeddle/notes-app:0.1 --push ."
  },
  "dependencies": {
    "connect-mongo": "^4.6.0",
    "dotenv": "^16.0.0",
    "express": "~4.16.1",
    "express-session": "^1.17.2",
    "mongodb": "^4.5.0",
    "passport": "^0.5.2",
    "passport-local": "^1.0.0"
  }
}
